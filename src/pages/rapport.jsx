import MainLayout from 'src/layouts/MainLayout';

const Rapport = () => {
    return (
        <div>
            <h1>Rapport Tracker (WIP)</h1>
        </div>
    );
};

Rapport.getLayout = (page) => <MainLayout>{page}</MainLayout>;
export default Rapport;
